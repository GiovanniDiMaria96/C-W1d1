namespace Oper_arit_log_bit
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Esercizio 1: Calcolatrice di Base (Operatori Aritmetici)

            #region Consegna
            /*
            1. Dichiara due variabili double, a e b, e inizializzale con valori a tua scelta (es. 10 e 3).
            2. Calcola la somma, la differenza, il prodotto e la divisione di a e b.
            3. Stampa ogni risultato con una descrizione chiara.
            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 2: Resto della Divisione (Operatore Modulo)

            #region Consegna
            /*
            1. Chiedi all'utente di inserire un numero intero.
            2. Usa l'operatore modulo (`%`) per calcolare il resto della divisione del numero per 2.
            3. Stampa se il numero è pari (resto 0) o dispari (resto diverso da 0).
            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 3: Incremento e Decremento

            #region Consegna
            /*
            1. Dichiara una variabile `int` chiamata `contatore` e inizializzala a `5`.
            2. Incrementa il suo valore di 1 usando l'operatore `++` e stampala.
            3. Decrementa il suo valore di 1 usando l'operatore `-` e stampala.            
            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 4: Controllo di un Intervallo (Operatore Logico AND)

            #region Consegna
            /*
            1. Chiedi all'utente di inserire un voto numerico (da 0 a 10).
            2. Scrivi un'espressione booleana che sia `true` solo se il voto è compreso tra 6 e 10 (inclusi).
            3. Stampa un messaggio che indica se il voto è sufficiente.          
            */
            #endregion

            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 5: Condizioni Alternative (Operatore Logico OR)

            #region Consegna
            /*
            1. Dichiara una `bool hasCoupon = false;` e un `DayOfWeek today = DayOfWeek.Monday;`.
            2. Scrivi un'espressione booleana che sia `true` se il cliente ha diritto allo sconto.
            3. Stampa il risultato. Prova a cambiare i valori delle variabili per testare la logica.            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 6: Inversione di Condizione (Operatore Logico NOT)
            #region Consegna
            /*
            1. Dichiara una variabile `bool isOnline = false;`.
            2. Usa l'operatore `!` per stampare un messaggio solo se l'utente NON è online.
            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 7: Controllo Permessi (Operatore Bitwise AND)

            #region Consegna
            /*
            Immagina che i permessi siano memorizzati in un `byte`: Lettura=1, Scrittura=2, Esecuzione=4. Un utente ha permessi di Lettura ed Esecuzione (1 + 4 = 5).
            1. Dichiara `byte permessiUtente = 5;` e `byte permessoScrittura = 2;`.
            2. Usa l'operatore `&` per verificare se l'utente ha il permesso di scrittura. Stampa il risultato.            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 8: Aggiunta Permessi (Operatore Bitwise OR)

            #region Consegna
            /*
            Partendo dai permessi dell'esercizio precedente (`permessiUtente = 5`), vogliamo aggiungere il permesso di scrittura.

            1. Usa l'operatore `|` per combinare `permessiUtente` con `permessoScrittura`.
            2. Stampa il nuovo valore dei permessi (dovrebbe essere 7).
            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 9: Inversione di un Permesso (Operatore Bitwise XOR)

            #region Consegna
            /*
            Un permesso può essere attivato o disattivato.

            1. Parti con `byte permessi = 5;` (Lettura + Esecuzione).
            2. Usa l'operatore `^` con il permesso di Esecuzione (4) per disattivarlo. Stampa il risultato (dovrebbe essere 1).
            3. Usa di nuovo l'operatore `^` con il permesso di Esecuzione (4) per riattivarlo. Stampa il risultato (dovrebbe essere 5).
            */
            #endregion
            #region Soluzione

            #endregion

            #endregion

            #region Esercizio 10: Moltiplicazione e Divisione Veloce (Operatori di Scorrimento)

            #region Consegna
            /*
            1. Dichiara un `int numero = 10;`.
            2. Usa l'operatore `<< 1` per moltiplicare il numero per 2.
            3. Usa l'operatore `>> 1` per dividere il numero originale per 2.
            4. Stampa i risultati.        
            */
            #endregion
            #region Soluzione

            #endregion

            #endregion
        }
    }
}
